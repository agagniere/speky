kind: requirements
category: functional
requirements:
- id: MCP012
  short: List requirements sorted by test coverage
  client_statement: |
    I would like to see which requirements are the least covered by tests, so I can
    prioritize writing test plans for them. I want the requirements with no tests at all
    to appear first, then those with few tests, then those that are only manually tested
    before the fully automated ones.
  long: |
    The user shall be able to retrieve a ranked list of requirements ordered by ascending
    test coverage.

    The MCP server shall expose a tool named `least_tested_requirements` that returns all
    requirements sorted so the least tested appear first.

    The sort order shall be:
    1. Ascending number of test plans (requirements with no tests come first)
    2. Ascending number of automated test plans (fully manual requirements come before
       partially or fully automated ones)
    3. Alphabetically by ID as a tiebreaker

    Each entry in the returned list shall contain:
    - `id`: The unique identifier
    - `short`: The short description (if present)
    - `category`: The requirement category
    - `test_plans`: Total number of tests that reference this requirement
    - `automated_test_plans`: Number of those tests that have at least one automated
      code test reference (a tagged code reference where the symbol is a test function)

    An optional `category` parameter allows filtering to a single category.
    An optional `tag` parameter allows filtering to requirements carrying a specific tag.
    Both filters may be combined.
    An optional `count` parameter limits the number of results returned (e.g. 5 returns
    only the 5 least tested requirements). Negative or out-of-range values shall be ignored.

    If the specified category or tag does not exist, the tool shall return an error.
  tags: [mcp:tools, mcp:query, mcp:traceability]
  ref: [MCP010]
  properties:
    since: '`0.4.0`'
    author: Claude
- id: MCP010
  short: Get test plan coverage for requirements
  client_statement: |
    I would like to know which requirements have no tests validating them, so I can
    identify coverage gaps. When preparing for a release or reviewing a specification,
    I need to quickly find what still needs test procedures written for it.
    I also want to distinguish between requirements that have only manual tests
    and those that are fully covered by automated tests.
  long: |
    The user shall be able to retrieve the test plan coverage status of requirements.

    The MCP server shall expose a tool named `test_plan_coverage` that partitions
    requirements into four groups:

    - `no_test_plan`: requirements for which no test references them in its `ref` field
    - `manual_test_plan`: requirements that have at least one test, but none of those
      tests have an associated automated code test reference
    - `partially_manual_test_plan`: requirements where some but not all associated tests
      have at least one automated code test reference
    - `automated_test_plan`: requirements where every associated test has at least one
      automated code test reference (a tagged code reference where the symbol is a test function)

    An optional `category` parameter allows filtering by category.

    Each group shall be a list of requirement summaries sorted alphabetically by ID,
    each containing:
    - `id`: The unique identifier
    - `short`: The short description (if present)
    - `category`: The requirement category
    - `tags`: List of tags (if present)
  tags: [mcp:tools, mcp:query, mcp:traceability]
  ref: [MCP005]
  properties:
    since: '`0.3.1`'
    version: 2
    author: Claude
- id: MCP007
  short: List requirements referencing another
  client_statement: |
    I would like to see which requirements reference or depend on a specific requirement
    to understand dependency chains and impact analysis. When a requirement changes, I need
    to know what other requirements might be affected.
  long: |
    The user shall be able to query which requirements reference a given requirement.

    The MCP server shall expose a tool named `list_references_to` that accepts
    a requirement ID and returns all requirements that reference it in their `ref` field.

    The response shall be a list of requirement summaries, each containing:
    - `id`: The unique identifier
    - `short`: The short description (if present)
    - `category`: The requirement category

    The list shall be sorted alphabetically by ID.

    If no requirements reference the given ID, an empty list shall be returned.
    If the requirement ID does not exist, an error shall be returned.
  tags: [mcp:tools, mcp:query, mcp:traceability]
  ref: [MCP003]
  properties:
    since: '`0.2.0`'
    author: Claude
